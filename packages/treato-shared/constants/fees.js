/**
 * @treato/shared — Fee Constants
 *
 * Financial constants shared between the backend earnings calculation
 * and the frontend earnings display. Change once, updates everywhere.
 */

/** Flat delivery fee earned per completed order (in INR) */
export const DELIVERY_FEE_PER_ORDER = 40;

/** Minimum payout threshold for withdrawal (in INR) */
export const MIN_WITHDRAWAL_AMOUNT = 200;
